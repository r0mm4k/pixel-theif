import { TPlayerActor } from '@/machines';

interface IPlayerProps {
  actor: TPlayerActor;
}

interface IImageStyledProps {
  left: string;
  top: string;
}

export type { IPlayerProps, IImageStyledProps };
